## 1.1 引言
所有操作系统都为它们运行的程序提供服务.

1.2 UNIX体系结构

操作系统是控制计算机硬件资源的软件，提供程序的运行环境。
 [操作系统的结构图]

#### 系统调用（system call）
 内核的接口被称为系统调用（system call） 图中阴影部分。

#### 公用函数库

 公用函数库构建在系统调用接口之上，
 应用程序既可使用公用函数库，也可以使用系统调用

#### shell

shell是一个特殊的应用程序，为运行其他应用程序提供可一个接口。
